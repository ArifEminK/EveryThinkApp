/**
 * Global Type Exports
 * Central export point for all TypeScript types
 */

export * from './theme';

// Export other types as we add them
export type { 
  Theme,
  Color,
  ThemeProps,
  WithTheme 
} from './theme';

