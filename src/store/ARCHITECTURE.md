# State Management Architecture Diagram

## 📊 Visual Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         COMPONENT LAYER                          │
│                      (EditItemScreen.tsx)                        │
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  Task Form   │  │Countdown Form│  │  Diary Form  │  ...     │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                  │                  │                   │
└─────────┼──────────────────┼──────────────────┼───────────────────┘
          │                  │                  │
          ▼                  ▼                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                          HOOKS LAYER                             │
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │useTaskState()│  │useCountdown()│  │useDiaryState()│  ...    │
│  │              │  │    State()   │  │              │          │
│  │ - title      │  │ - title      │  │ - title      │          │
│  │ - setTitle() │  │ - setTitle() │  │ - setTitle() │          │
│  │ - reset()    │  │ - reset()    │  │ - reset()    │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                  │                  │                   │
└─────────┼──────────────────┼──────────────────┼───────────────────┘
          │                  │                  │
          └──────────────────┼──────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                        CONTEXT LAYER                             │
│                     (EditItemContext)                            │
│                                                                   │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Context Value:                                           │  │
│  │  - state: RootState                                       │  │
│  │  - dispatch: Dispatch<RootAction>                         │  │
│  │  - actions: { task, countdown, diary, alarm }            │  │
│  └───────────────────────────────────────────────────────────┘  │
│                             │                                    │
└─────────────────────────────┼────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        REDUCER LAYER                             │
│                                                                   │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                      rootReducer()                        │  │
│  │                                                           │  │
│  │  Delegates actions to:                                   │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐     │  │
│  │  │taskReducer()│  │countdown    │  │diaryReducer()│ ... │  │
│  │  │             │  │Reducer()    │  │             │     │  │
│  │  └─────────────┘  └─────────────┘  └─────────────┘     │  │
│  └───────────────────────────────────────────────────────────┘  │
│                             │                                    │
└─────────────────────────────┼────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                         STATE LAYER                              │
│                                                                   │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                       RootState                           │  │
│  │                                                           │  │
│  │  {                                                        │  │
│  │    task: TaskState,                                      │  │
│  │    countdown: CountdownState,                            │  │
│  │    diary: DiaryState,                                    │  │
│  │    alarm: AlarmState                                     │  │
│  │  }                                                        │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow

```
┌─────────────┐
│   USER      │
│  ACTION     │
└──────┬──────┘
       │
       │ (e.g., types in input field)
       │
       ▼
┌─────────────────────────────────┐
│  Component calls setter         │
│  taskState.setTitle("New Title")│
└──────┬──────────────────────────┘
       │
       │ Hook creates action
       │
       ▼
┌─────────────────────────────────┐
│  Action Creator                 │
│  taskActions.setTitle(title)    │
│  Returns: { type, payload }     │
└──────┬──────────────────────────┘
       │
       │ Dispatch action
       │
       ▼
┌─────────────────────────────────┐
│  Context dispatch()             │
│  dispatch(action)               │
└──────┬──────────────────────────┘
       │
       │ Route to reducer
       │
       ▼
┌─────────────────────────────────┐
│  rootReducer()                  │
│  Checks action.type prefix      │
└──────┬──────────────────────────┘
       │
       │ Delegate to module reducer
       │
       ▼
┌─────────────────────────────────┐
│  taskReducer()                  │
│  switch(action.type)            │
│  Returns new state              │
└──────┬──────────────────────────┘
       │
       │ New state
       │
       ▼
┌─────────────────────────────────┐
│  Context updates state          │
│  Re-render components           │
└──────┬──────────────────────────┘
       │
       │ Component receives new state
       │
       ▼
┌─────────────────────────────────┐
│  UI Updates                     │
│  Input shows "New Title"        │
└─────────────────────────────────┘
```

## 🗂️ File Organization

```
src/store/
│
├── 📁 types/                    # TypeScript Definitions
│   ├── taskState.ts             # Task state & action types
│   ├── countdownState.ts        # Countdown state & action types
│   ├── diaryState.ts            # Diary state & action types
│   ├── alarmState.ts            # Alarm state & action types
│   ├── rootState.ts             # Combined root state type
│   └── index.ts                 # Export all types
│
├── 📁 reducers/                 # State Reducers
│   ├── taskReducer.ts           # Task state reducer
│   ├── countdownReducer.ts      # Countdown state reducer
│   ├── diaryReducer.ts          # Diary state reducer
│   ├── alarmReducer.ts          # Alarm state reducer
│   ├── rootReducer.ts           # Combined root reducer
│   └── index.ts                 # Export all reducers
│
├── 📁 actions/                  # Action Creators
│   ├── taskActions.ts           # Task action creators
│   ├── countdownActions.ts      # Countdown action creators
│   ├── diaryActions.ts          # Diary action creators
│   ├── alarmActions.ts          # Alarm action creators
│   └── index.ts                 # Export all actions
│
├── 📁 context/                  # React Context
│   ├── EditItemContext.tsx      # Context provider & hooks
│   └── index.ts                 # Export context
│
├── 📁 hooks/                    # Custom Hooks
│   ├── useTaskState.ts          # Task state hook
│   ├── useCountdownState.ts     # Countdown state hook
│   ├── useDiaryState.ts         # Diary state hook
│   ├── useAlarmState.ts         # Alarm state hook
│   └── index.ts                 # Export all hooks
│
├── 📄 index.ts                  # Main store export
├── 📄 README.md                 # Documentation
├── 📄 examples.tsx              # Usage examples
└── 📄 ARCHITECTURE.md           # This file
```

## 🎯 Module Structure

Each module (Task, Countdown, Diary, Alarm) follows the same pattern:

```
Module (e.g., Task)
│
├── 📄 types/taskState.ts
│   ├── TaskState interface
│   ├── TaskActionType enum
│   └── TaskAction type
│
├── 📄 reducers/taskReducer.ts
│   ├── initialTaskState
│   └── taskReducer function
│
├── 📄 actions/taskActions.ts
│   └── taskActions object
│       ├── setTitle()
│       ├── setDescription()
│       ├── setField()
│       ├── reset()
│       └── initialize()
│
└── 📄 hooks/useTaskState.ts
    └── useTaskState() hook
        ├── Returns state values
        ├── Returns setter functions
        └── Returns utility functions
```

## 🔐 Type Safety Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                     TypeScript Type Safety                       │
└─────────────────────────────────────────────────────────────────┘

Component
    │
    │ const taskState = useTaskState()
    │                      ↓
    │                 Returns: TaskState + Setters
    │
    ├─→ taskState.title
    │       ↓
    │   Type: string ✓
    │
    ├─→ taskState.setTitle("text")
    │       ↓
    │   Expects: string ✓
    │   taskState.setTitle(123) ✗ Type Error!
    │
    ├─→ taskState.setCategory(TASK_CATEGORY.WORK)
    │       ↓
    │   Expects: TaskCategory ✓
    │   taskState.setCategory("invalid") ✗ Type Error!
    │
    └─→ taskState.setField("title", "text")
            ↓
        field: keyof TaskState ✓
        value: any (but validated by reducer) ✓
```

## 🚀 Performance Optimizations

```
┌─────────────────────────────────────────────────────────────────┐
│                    Performance Features                          │
└─────────────────────────────────────────────────────────────────┘

1. Memoization in Context
   ┌────────────────────────────────────┐
   │ useMemo(() => ({                   │
   │   state,                           │
   │   dispatch,                        │
   │   actions                          │
   │ }), [state, dispatch, actions])    │
   └────────────────────────────────────┘
   ↓ Prevents unnecessary re-renders

2. Memoized Action Creators
   ┌────────────────────────────────────┐
   │ const actions = useMemo(           │
   │   () => ({ task, countdown, ... }),│
   │   []                               │
   │ )                                  │
   └────────────────────────────────────┘
   ↓ Actions created once

3. Memoized Setters in Hooks
   ┌────────────────────────────────────┐
   │ const setTitle = useCallback(      │
   │   (title) => dispatch(...),        │
   │   [dispatch, actions.task]         │
   │ )                                  │
   └────────────────────────────────────┘
   ↓ Stable function references

4. Modular State Updates
   ┌────────────────────────────────────┐
   │ Only affected module state updates │
   │ Other modules remain unchanged     │
   └────────────────────────────────────┘
   ↓ Minimal re-renders
```

## 🧩 Extension Points

```
┌─────────────────────────────────────────────────────────────────┐
│                    How to Extend                                 │
└─────────────────────────────────────────────────────────────────┘

1. Add New Module (e.g., Note)
   │
   ├─→ Create types/noteState.ts
   ├─→ Create reducers/noteReducer.ts
   ├─→ Create actions/noteActions.ts
   ├─→ Create hooks/useNoteState.ts
   ├─→ Add to rootState.ts
   ├─→ Add to rootReducer.ts
   └─→ Export from index files

2. Add New Action to Existing Module
   │
   ├─→ Add action type to types/taskState.ts
   ├─→ Add handler to reducers/taskReducer.ts
   ├─→ Add creator to actions/taskActions.ts
   └─→ Add setter to hooks/useTaskState.ts (optional)

3. Add Middleware
   │
   └─→ Wrap dispatch in EditItemContext.tsx
       const enhancedDispatch = (action) => {
         console.log('Action:', action);
         dispatch(action);
       };
```

## 📈 Scalability

This architecture scales well because:

1. **Modular**: Each module is independent
2. **Type-Safe**: TypeScript catches errors at compile time
3. **Testable**: Each layer can be tested independently
4. **Maintainable**: Clear separation of concerns
5. **Extensible**: Easy to add new modules or features
6. **Performance**: Optimized with memoization
7. **Developer-Friendly**: Intuitive API and good DX

---

**Architecture designed for production use** 🚀
